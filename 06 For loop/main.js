// For Loop Intregation 

for (var i = 0; i < 5; i++) {
    console.log(i);
  }

  // even and envent number checking using for loop 

  for(var interValue = 0; interValue <= 20; interValue++)
  {
    if(interValue % 2 == 0)
    {
        console.log('This is even number : ',interValue); 
    }
    else{
        console.log('This is odd numberr :',interValue); 
    }
  }